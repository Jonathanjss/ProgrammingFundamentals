function evenOrOdd(number){
  let i;
  for(i=0; i<=number; i++){
    i == 0 ? console.log(`Numero ${i}`) : i%2 == 0 ? console.log(`El numero ${i} es par`): console.log(`El numero ${i} es impar`)
  }
}
evenOrOdd(200);